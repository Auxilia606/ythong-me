# Pages Router Disabled

This directory intentionally contains no route files.

It exists to make the repository's Pages Router boundary explicit while the app uses the Next.js App Router under `app/`.
